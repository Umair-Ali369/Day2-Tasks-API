# Day 2 - Task API (Express.js)

This is my **Day 2 backend project**, where I built a fully functional CRUD Task API using **Node.js + Express**.  
The goal of this project was to understand how APIs work using Express, request handling, routes, and JSON responses.

---

## 🚀 Features

- Get all tasks  
- Get a single task by ID  
- (POST) Create a new task  
- (PUT) Update an existing task  
- Delete a task  
- In-memory database (array)  
- Clean and simple Express structure  
- JSON API responses  

---

▶ How to Run the Project
1️⃣ Install dependencies - npm install
2️⃣ Start the server - node index.js
3️⃣ Server runs on: http://localhost:5001

---
📌 API Endpoints
GET /tasks
GET /tasks/:id
POST /tasks
PUT /tasks/:id
DELETE /tasks/:id
